Remove useless inline specifiers

Signed-off-by: Mark Charlebois <charlebm@gmail.com>
---
 fs/bio.c             |    2 +-
 fs/block_dev.c       |    2 +-
 fs/buffer.c          |    2 +-
 kernel/time.c        |    4 ++--
 mm/filemap.c         |    2 +-
 net/core/dev.c       |    2 +-
 net/ipv4/ip_output.c |    2 +-
 7 files changed, 8 insertions(+), 8 deletions(-)

Index: linux/fs/bio.c
===================================================================
--- linux.orig/fs/bio.c	2012-09-05 12:03:39.829231060 -0400
+++ linux/fs/bio.c	2012-09-05 12:10:33.644028910 -0400
@@ -428,7 +428,7 @@
 }
 EXPORT_SYMBOL(bio_put);
 
-inline int bio_phys_segments(struct request_queue *q, struct bio *bio)
+int bio_phys_segments(struct request_queue *q, struct bio *bio)
 {
 	if (unlikely(!bio_flagged(bio, BIO_SEG_VALID)))
 		blk_recount_segments(q, bio);
Index: linux/fs/block_dev.c
===================================================================
--- linux.orig/fs/block_dev.c	2012-09-05 12:03:39.833231010 -0400
+++ linux/fs/block_dev.c	2012-09-05 12:10:33.644028910 -0400
@@ -42,7 +42,7 @@
 	return container_of(inode, struct bdev_inode, vfs_inode);
 }
 
-inline struct block_device *I_BDEV(struct inode *inode)
+struct block_device *I_BDEV(struct inode *inode)
 {
 	return &BDEV_I(inode)->bdev;
 }
Index: linux/fs/buffer.c
===================================================================
--- linux.orig/fs/buffer.c	2012-09-05 12:03:39.833231010 -0400
+++ linux/fs/buffer.c	2012-09-05 12:10:33.644028910 -0400
@@ -46,7 +46,7 @@
 
 #define BH_ENTRY(list) list_entry((list), struct buffer_head, b_assoc_buffers)
 
-inline void
+void
 init_buffer(struct buffer_head *bh, bh_end_io_t *handler, void *private)
 {
 	bh->b_end_io = handler;
Index: linux/kernel/time.c
===================================================================
--- linux.orig/kernel/time.c	2012-09-05 12:03:39.841230908 -0400
+++ linux/kernel/time.c	2012-09-05 12:10:33.644028910 -0400
@@ -232,7 +232,7 @@
  * Avoid unnecessary multiplications/divisions in the
  * two most common HZ cases:
  */
-inline unsigned int jiffies_to_msecs(const unsigned long j)
+unsigned int jiffies_to_msecs(const unsigned long j)
 {
 #if HZ <= MSEC_PER_SEC && !(MSEC_PER_SEC % HZ)
 	return (MSEC_PER_SEC / HZ) * j;
@@ -248,7 +248,7 @@
 }
 EXPORT_SYMBOL(jiffies_to_msecs);
 
-inline unsigned int jiffies_to_usecs(const unsigned long j)
+unsigned int jiffies_to_usecs(const unsigned long j)
 {
 #if HZ <= USEC_PER_SEC && !(USEC_PER_SEC % HZ)
 	return (USEC_PER_SEC / HZ) * j;
Index: linux/mm/filemap.c
===================================================================
--- linux.orig/mm/filemap.c	2012-09-05 12:03:39.845230857 -0400
+++ linux/mm/filemap.c	2012-09-05 12:10:33.644028910 -0400
@@ -2073,7 +2073,7 @@
  * Returns appropriate error code that caller should return or
  * zero in case that write should be allowed.
  */
-inline int generic_write_checks(struct file *file, loff_t *pos, size_t *count, int isblk)
+int generic_write_checks(struct file *file, loff_t *pos, size_t *count, int isblk)
 {
 	struct inode *inode = file->f_mapping->host;
 	unsigned long limit = rlimit(RLIMIT_FSIZE);
Index: linux/net/core/dev.c
===================================================================
--- linux.orig/net/core/dev.c	2012-09-05 12:03:39.849230806 -0400
+++ linux/net/core/dev.c	2012-09-05 12:10:33.648028840 -0400
@@ -3444,7 +3444,7 @@
 	return netif_receive_skb(skb);
 }
 
-inline void napi_gro_flush(struct napi_struct *napi)
+void napi_gro_flush(struct napi_struct *napi)
 {
 	struct sk_buff *skb, *next;
 
Index: linux/net/ipv4/ip_output.c
===================================================================
--- linux.orig/net/ipv4/ip_output.c	2012-09-05 12:03:39.849230806 -0400
+++ linux/net/ipv4/ip_output.c	2012-09-05 12:10:33.648028840 -0400
@@ -84,7 +84,7 @@
 EXPORT_SYMBOL(sysctl_ip_default_ttl);
 
 /* Generate a checksum for an outgoing IP datagram. */
-__inline__ void ip_send_check(struct iphdr *iph)
+void ip_send_check(struct iphdr *iph)
 {
 	iph->check = 0;
 	iph->check = ip_fast_csum((unsigned char *)iph, iph->ihl);
